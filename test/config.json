{
    "name": "smile",
    "version": "0.0.1",
    "description": "config example",
    "db": {
        "postgres": {
            "trackingDocument": {
                "database": "tracking",
                "host": "localhost:5432",
                "user": "gio",
                "password": "gio",
                "sslmode": "disable"
            }
        }
    },
    "http": {
        "listen": "localhost",
        "port": "8080"
    },
    "upstream": {
        "http": {
            "orcinus": {
                "address": "localhost",
                "port": "",
                "schema": "http://"
            }
        }
    }
}